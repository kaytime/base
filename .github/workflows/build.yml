name: Build RootFS

on:
  push:
    branches:
      - "stable"
      - "unstable"
      - "testing"
    paths-ignore:
      - "README.md"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          echo "${{toJSON(github.event)}}"
          chmod +x gh-build.sh
          mkdir ouputs
          sudo ./gh-build.sh amd64
          sudo ./gh-build.sh arm64

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.head_commit.message }}
          files: |
            *.tar.xz
